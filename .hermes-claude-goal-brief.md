/goal In /root/.hermes/scratch/hermes-ide-sync2 (branch main, mid-merge with 4 unresolved conflicts), finish with: (A) the merge committed so HEAD's parents are exactly [72cf74e4f4b5f686b4a09b0f657c2ce1aae7c30c, d10bb2ab6fb9e16028070bf7075284a7b0476872], (B) the targeted test trio passing locally (exit 0), (C) main pushed to origin (https://github.com/QuixThe2nd/hermes-ide.git, NEVER --force), (D) workflow fork-sync.yml dispatched and its new run either green or failed at a NEW upstream sha (not d10bb2ab6f).

WORK ONLY in /root/.hermes/scratch/hermes-ide-sync2. Never touch /root/hermes-agent or any other repo. Commit identity on EVERY commit: git -c user.name='Hermes Agent' -c user.email='hermes@yazdani.au'.

CONTEXT: This scratch clone is mid-merge importing upstream commit d10bb2ab6fb9e16028070bf7075284a7b0476872 ("test: make tests/ mirror the source tree") onto fork main 72cf74e4f4 (a GitHub Actions fork-sync run stalled on exactly these conflicts; you are completing that import by hand). Network commands against this repo are slow: prefix fetch/push with `timeout 550 `; the clone already has http.lowSpeedLimit configured.

STEP 0 — verify state, STOP if wrong:
- `git rev-parse HEAD` must print 72cf74e4f4b5f686b4a09b0f657c2ce1aae7c30c
- `cat .git/MERGE_HEAD` must print d10bb2ab6fb9e16028070bf7075284a7b0476872
- `git status --porcelain | grep -E '^(UU|AU)'` must list EXACTLY: hermes_cli/default_soul.py (UU), tests/agent/test_gateway_restart_terminal_turn.py, tests/agent/test_live_retry_progress.py, tests/agent/test_no_undefined_bare_undefined_bare_names? NO — exactly these three AU paths: tests/agent/test_gateway_restart_terminal_turn.py, tests/agent/test_live_retry_progress.py, tests/agent/test_no_undefined_bare_names.py
If any check fails, STOP and report exact output.

STEP 1 — resolve hermes_cli/default_soul.py: the file has ONE conflict block. Delete the entire block from the line `<<<<<<< HEAD` through the line `>>>>>>> d10bb2ab6fb9e16028070bf7075284a7b0476872` inclusive, and replace it with EXACTLY these lines (keep the fork's credential-preflight legacy-soul entry, take upstream's shorter comment for the ASCII variant):
```
    # ASCII-dashed variant seeded by scripts/install.ps1 (must stay pure ASCII, see
    # tests/scripts/install/test_install_ps1_ascii_only.py); upgrading converges Windows installs on the em-dash text.
```
(The block you are removing contains: `<<<<<<< HEAD`, then comment `# Fork auto-seed: pre-#95681 identity plus the credential-preflight`, then a parenthesized long string mentioning ~/.hermes/secrets/ and shell variables, then a multi-line comment about the ASCII-dashed variant, then `=======`, then upstream's two-line ASCII comment, then `>>>>>>> d10bb2ab6f...`. The line AFTER the removed block is `    DEFAULT_SOUL_MD.replace("\u2014", "--"),` and must remain.) Verify `grep -c '<<<<<<<' hermes_cli/default_soul.py` is 0, then `git add hermes_cli/default_soul.py`.

STEP 2 — resolve the three location conflicts: for each of tests/agent/test_gateway_restart_terminal_turn.py, tests/agent/test_live_retry_progress.py, tests/agent/test_no_undefined_bare_names.py: confirm the file exists on disk (it was added by HEAD inside the old tests/run_agent/ dir, which upstream renamed to tests/agent/; git already staged it at the new path), then `git add <path>`. If a file is missing from disk, STOP and report.

STEP 3 — verify clean: `git status --porcelain | grep -E '^(UU|AA|DU|UD|AU|UA|DD)'` must be empty; `git diff --cached --check` must output nothing.

STEP 4 — commit: `git -c user.name='Hermes Agent' -c user.email='hermes@yazdani.au' commit --no-edit` (MERGE_MSG already holds the correct message "Merge upstream d10bb2ab6f: ..."). Then `git log -1 --format='%H %P'` — parents must be exactly 72cf74e4f4b5f686b4a09b0f657c2ce1aae7c30c and d10bb2ab6fb9e16028070bf7075284a7b0476872. Do not amend or reword.

STEP 5 — tests (must exit 0): `uv run --frozen pytest tests/gateway/test_webhook_adapter.py tests/gateway/test_discord_plugin_setup.py tests/hermes_cli/test_send_cmd.py -x -q`. If this fails after an honest retry, STOP and report the output.

STEP 6 — push: `timeout 550 git push origin main`. NEVER use --force. If rejected (another fork PR landed on main concurrently — this happened today): `timeout 550 git fetch origin main`, then `git reset --hard origin/main`, re-run `timeout 550 git merge --no-ff d10bb2ab6fb9e16028070bf7075284a7b0476872 -m "Merge upstream d10bb2ab6f: test: make tests/ mirror the source tree; drop issue numbers from filenames"` and redo steps 1-5. Max 2 race retries, then STOP and report.

STEP 7 — verify push landed: `git ls-remote origin refs/heads/main` must show your merge sha.

STEP 8 — dispatch the workflow and confirm: `gh workflow run fork-sync.yml --repo QuixThe2nd/hermes-ide --ref main`, then poll `gh run list --repo QuixThe2nd/hermes-ide --workflow fork-sync.yml --limit 1` until a run with databaseId > 34828637116 appears, then poll its status up to 10 minutes until completion. Success = conclusion "success"; acceptable = "failure" at a NEW upstream sha (fetch the logs via `gh api repos/QuixThe2nd/hermes-ide/actions/runs/<id>/jobs` and report the failing sha + conflict paths). Failure at d10bb2ab6f again = STOP and report.

HARD RULES: never force-push; never delete or re-clone the scratch dir; any step failing twice after honest retries = STOP and report with the exact commands and output. Return concise English only.