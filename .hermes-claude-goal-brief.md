/goal Implement key-manager mode for the llm_usage_proxy plugin in /root/.hermes/scratch/llm-usage-proxy (branch feat/llm-usage-proxy, HEAD 8b418252513f614a4bcf909c8b87c8c037de9c92, PR #215 already open on this SHA — build on top, do not rebase or amend existing commits).

INVARIANT (observable end state): an OpenAI-compatible or Anthropic-compatible client on this host can be configured with base_url = http://127.0.0.1:<port>/p/<route> and a LOCAL proxy token (not the real provider key), and its requests succeed because the proxy injects the real upstream credential. Provider API keys live in ONE root-only store owned by the proxy; clients never hold provider keys. Every ledger row records WHICH caller token made the request.

EXISTING MECHANISM (adapt, don't replace): plugins/llm_usage_proxy/server.py already implements the relay: GET/POST to /p/<upstream_name>/<rest> forwards to the registered base URL via _proxy()/_forward_headers() (hop-by-hop stripped, everything else including Authorization passed through untouched), usage parsed into SQLite table usage_events (schema in server.py ~line 95). Route table built in routes.py build_route_table() from explicit config upstreams. CLI: plugins/llm_usage_proxy/cli.py (enable/disable/status). Unit rendering: plugins/llm_usage_proxy/systemd.py build_exec_start_argv(). Identity verification: existing --identity flag + /health.

REQUIRED BEHAVIOR:
1. Key store: /root/.hermes/usage-proxy/keys.json (0600, dir 0700) mapping route name -> list of provider API keys (multi-key = rotation). Manageable via `hermes llm_usage_proxy keys set <route> --key <value>` (repeatable for multiple), `keys list` (shows route names + key fingerprints only, NEVER full keys), `keys remove <route> [--index N]`. Never log full key values; redact via existing redact_text.
2. Auth injection: when a route has keys in the store AND --manage-keys is passed to the server (add via unit argv), the proxy REPLACES the incoming Authorization header (Bearer) / x-api-key with the store's key for that route (round-robin per route, thread-safe, on 401/429 try next key once per request). Without --manage-keys, current passthrough behavior unchanged.
3. Caller tokens: store also maps caller-name -> random token (secrets.token_urlsafe(24)). `hermes llm_usage_proxy callers create <name>` prints token once. When a request arrives with Authorization: Bearer <caller-token> (or X-Usage-Caller-Token header), the proxy validates it against the store, records the caller name in a NEW usage_events column `caller TEXT` (ALTER TABLE migration for existing DBs, backfill NULL), then strips/swaps auth per rule 2. Unknown token + manage-keys on = 401. No tokens configured = accept anything (compat with current Hermes in-process usage).
4. GET /p/<route>/models and /p/<route>/v1/models forwarded like any request so client model pickers work.
5. Ledger/API: caller column appears in /api/events JSON. Web UI in /root/.hermes/scratch/usage-proxy-webui/server.py may show it later — do not touch it in this task.
6. Docs: update plugins/llm_usage_proxy README section + scratch README.md: one paragraph "key-manager mode" with the 3 client recipes (OpenAI-compatible base_url+p token; Anthropic base_url via ANTHROPIC_BASE_URL=http://127.0.0.1:8790/p/zai-anthropic + ANTHROPIC_AUTH_TOKEN=<caller token>; Hindsight HINDSIGHT_API_LLM_BASE_URL). State OAuth providers (codex, xai-oauth) are NOT manageable — no static key.

CONSTRAINTS: stdlib only in server path; no new deps; all existing tests must stay green (canonical: HERMES_PYTHON=/root/hermes-agent/.venv/bin/python bash scripts/run_tests.sh -j 4 tests/plugins/llm_usage_proxy tests/agent/test_provider_client_seam.py tests/agent/test_anthropic_adapter.py — currently 154 passed at this HEAD); add focused tests for key store file perms, auth swap, caller validation, round-robin+401-retry, /models forward, schema migration. Secrets never in argv except at `keys set` time (document). Commit with clear message on feat/llm-usage-proxy. TASK.md/RESUME.md/FINISH.md stay untracked. Report final HEAD SHA + test summary + files changed.