# Continue the LLM usage proxy, do not restart from scratch

This brief SUPERSEDES conflicting design requirements in TASK.md. The user's request is a reverse proxy between Hermes and Codex/GLM/Kimi/etc to properly measure token usage. Product delta: plugins/llm_usage_proxy plus the minimal HTTP-client seams captures per-request provider-reported tokens without changing provider identity, authentication, API mode, or other profiles. No dashboard, new dependencies, global monkeypatch, MITM, wrapper edits, or unrelated infrastructure.

## Verified recovery

Workspace /root/.hermes/scratch/llm-usage-proxy, branch feat/llm-usage-proxy, base 556a6049a7e37025eaf1e7976d9046b5e3dd08ac. Previous Claude run timed out after 107 turns. Existing changes are uncommitted: 11 plugin files, 2 test files and edits to hermes_cli/{auth,providers,runtime_provider}.py. README remains untouched. Parent ran HERMES_PYTHON=/root/hermes-agent/.venv/bin/python bash scripts/run_tests.sh tests/plugins/llm_usage_proxy: 1 file,24 tests passed,0 failed, exit0. These tests do not cover integration or the flaws below. Recovery archive /root/.hermes/reviews/usage-proxy-recovery-X4UDFl/{tracked.patch,new-files.tgz}. Preserve the viable work.

First write /root/.hermes/reviews/usage-proxy-implementation.status.json with status,phase,updated_at,detail; update before each major phase, errors and exit. Check DEBT/NOTE markers. Work sequentially, no nested agents. Do not read giant whole AGENTS/runtime files; target relevant functions. Inspect at most necessary narrow seams before starting. Checkpoint after each completed area so another timeout doesn't erase progress.

## Correct the routing design first

Delete process-wide provider os.environ rewrites. Revert only this task's new Codex override code if no longer needed. Original base URLs MUST remain inputs to provider/api-mode/auth/account/pool resolution and SDK base_url, including URL-based Codex Cloudflare/account headers and Kimi protocol selection. No opt-out profile should inherit routing; no edits to other profiles or their secrets/config.

Known common seam: agent/process_bootstrap.py::build_keepalive_http_client(base_url,async_mode=False,verify=True) (lines522+), used by main through agent._build_keepalive_http_client and auxiliary through agent/auxiliary_client.py::_openai_http_client_kwargs (232+). Existing sync transport pool shared views and async transport ownership must remain correct. Anthropic client builder agent/anthropic_adapter.py::build_anthropic_client has a separate SDK construction path; add only the minimal injection needed there. Do not invent a parallel provider resolver. Prefer a scoped, per-client HTTP transport wrapper that rewrites the final request destination to /p/<configured-route> after real auth/protocol decisions. Preserve response.request's logical provider URL, Host handling, redirect semantics, exact path/query and headers. Cover sync AND async. No new broad/core tool. The proxy targets trusted configured real upstreams, never an arbitrary request-supplied URL. Route only when original request origin+path matches the configured target with a real path boundary; unchanged unmatched traffic and requests for different hosts. No loops. Respect TLS verification/proxy policy: don't silently change custom CA/SSL settings; unsupported cases should visibly remain unmetered.

Plugin default_enabled can expose CLI, but inference routing/service activation must be explicit per-profile config (enabled false until operator enables) and must not be enabled by global env. Plugin disabled gates honored. Loopback127.0.0.1:8790; CLI serve/status/enable/disable/reconcile, summary/recent usage existing endpoints. systemd only service reconciliation, never install/start during tests/import alone. Protect named profile service/port ownership: never adopt another profile's listener or stop it. A generic health {ok:true} is insufficient identity to trust with bearer credentials: verify service/protocol and route mapping identity. Capture real endpoints from profile-scoped config/env/credential endpoint resolution, not process-global leaked env. Kimi /coding (Anthropic) versus /coding/v1 (OpenAI) must preserve whichever Hermes actually selects. GLM generic vs coding URL must not change an account's route. Startup healthy is not runtime failover: if bypass used, expose degraded/unmetered state and never count bypass as zero. NEVER auto-replay a request direct after it might have reached upstream (double billing). Keep failures honest rather than claiming guaranteed fail-open. Existing clients may require restart; document exactly what enable/disable affects.

## Correct observable server flaws

- server.py:765 resp.read(CHUNK_SIZE) fills up to64KiB, delaying small SSE messages. Use available-byte reads e.g. read1 + flush. Test first SSE frame arrives BEFORE fake upstream releases second frame, not just final concatenated equality.
- Accounting: Anthropic input_tokens excludes cache reads/writes, so normalized prompt/total must include components exactly once; OpenAI/Responses cached/reasoning fields are SUBSETS, never added twice. Preserve separate cache creation/read fields. Missing token fields remain null. Summary returns known sums with counts for unknown/partial requests; don't present unknown as measured zero.
- usage completeness and request outcome must be separate: completed/aborted/upstream_error and final/partial/missing, terminal event rules for OpenAI, Responses and Anthropic. Cumulative snapshots never summed, repeated terminal events never double counted, each HTTP attempt is one row. Interrupted streams retain whatever genuine usage was observed without calling it authoritative final. Errors/timeouts captured in finally (current code discards scanner usage on exceptions).
- SSE parser handles arbitrary byte splits, split UTF8, multi-data-line events and line endings, bounded per-event memory. Compressed streams have bounded decoding. Do not corrupt forwarded payloads. A parser error can't break a valid stream or silently claim success.
- No raw request/query logging, no bodies or credentials in errors/metadata. Disable raw access logs instead of relying on credential regex. Validate upstream URLs before any log or systemd argv: no userinfo/credential queries/fragments, self-target, invalid ports. Persist bounded safe metadata only. 0600 DB/WAL/SHM from creation (UMask0077/unit); do not log raw upstream exception text likely to contain private data.
- Correct HTTP framing on truncation, HEAD/no-body statuses and failures; strip response Connection-nominated headers too. Preserve cancellation cleanup. Do not create new endpoint registry/dashboard.

## Proof and handoff

Use existing pytest, no new deps/runners/lockfile churn. Focused regression tests must prove actual Hermes main, pooled rotation and auxiliary client construction route through fake upstream without changing auth, API mode or global env; sync+async, named profile disabled, foreign health listener, missing/final/aborted usage, early stream frame, repeated events, secrets absent, safe disable/reconcile. Prefer bounded monkeypatches around credentials/network DNS while real clients/HTTP/SQLite run. Keep new tests out of product commit if diff becomes test-dominated; proof can live out-of-band.

Also create a PRIVATE untracked operational smoke script under /root/.hermes/reviews/usage-proxy-smoke.py (outside product). Document exact flags in REPORT.md. It should exercise real candidate Hermes provider clients (no new agent process/loop), use existing profile-scoped credentials locally without printing values, one tiny request per selected provider, explicit provider+model arguments, max output2048 and120s timeout, no fallback concealment/retries, report selected real base/api mode/model, metered route/request id and provider usage vs DB equality. Bind imports to this candidate. Do NOT run live smoke yourself, parent owns it. No prompts or credentials in logs. Author script only if needed for existing-client integration proof, not a second product runner.

Update README beside quota wall. Say provider-reported usage, NOT independent tokenizer/billing truth; limits explicit: bypassed traffic, opaque cloud runs, providers that omit usage, external CLI wrappers. No service auto-on claims while disabled.

Run canonical HERMES_PYTHON=/root/hermes-agent/.venv/bin/python bash scripts/run_tests.sh -j 4 tests/plugins/llm_usage_proxy plus nearest existing tests for EVERY touched core seam. Run git diff --check and relevant lint. Record exact commands/results/failed baseline proof in /root/.hermes/reviews/usage-proxy-implementation.REPORT.md. Keep TASK.md,RESUME.md,status/harness/reports untracked/outside commit. Commit product locally as Hermes Agent <hermes@yazdani.au> only after tests. Do not push/open PR/deploy/restart/change live config or credential files. /root/hermes-agent must stay untouched. End with concise SHA, changed files, verifier commands, smoke flags, blockers. No more exploration after report.
