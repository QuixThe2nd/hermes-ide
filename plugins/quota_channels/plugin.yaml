name: quota_channels
version: 1.4.0
description: "Discord voice-channel model quota display for Codex, Kimi, z.ai (per credential-pool wallet), Cursor, and Grok under a Models category — ordered by the fallback score (quota x reset urgency x uptime), with automatic 7-day token enrichment on Codex, z.ai, and Cursor."
author: Hermes Agent
kind: backend
provides_tools:
  - quota_channels_tick
