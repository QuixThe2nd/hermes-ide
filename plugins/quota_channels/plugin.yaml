name: quota_channels
version: 1.3.0
description: "Discord voice-channel model quota display for Codex, Kimi, z.ai, Cursor, Grok, and OpenRouter — one channel per provider under a Models category, ordered by the fallback score (quota x reset urgency x uptime), with automatic 7-day token enrichment on Codex, z.ai, and Cursor and a virtual unlimited OpenRouter row."
author: Hermes Agent
kind: backend
provides_tools:
  - quota_channels_tick
