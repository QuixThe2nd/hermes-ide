name: dev_pipeline
default_enabled: true
version: 1.0.0
description: "Durable automated development pipeline — delegate_development(repo, task) plans via the MoA council, executes through the Cursor CLI in sandboxed systemd attempts, verifies mechanically, reviews with a Russian-language Kimi K3 + Grok 4.5 pair, and opens a draft PR on pass. Jobs survive restarts via the Kanban DB. The executor systemd user service self-installs on plugin load (Linux/systemd user scope)."
author: QuixThe2nd
kind: backend
provides_tools:
  - delegate_development
  - dev_pipeline_status
