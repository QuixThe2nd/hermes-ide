name: live-progress
version: 1.0.0
description: "Minimal renderer for display.tool_progress: plugin — one bubble per turn with a header, one line per tool call and a footer that counts up live."
author: "Huhn (reference implementation for the plugin progress mode)"
hooks:
  - pre_llm_call
  - post_tool_call
  - post_llm_call
  - on_session_end
