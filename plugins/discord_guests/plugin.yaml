name: discord_guests
default_enabled: true
version: 1.0.0
description: "Inviting a guest auto-creates a private lounge channel under the Lounges category — only that member (plus whoever already sees Lounges) can view it; @everyone stays view-denied everywhere."
author: Hermes Agent
kind: standalone
provides_tools:
  - discord_guests
