"""Fallback quota reorder plugin — reorder fallback_providers from Discord channel names."""

from __future__ import annotations


def register(ctx) -> None:
    """CLI/cron-driven plugin; no model tools registered."""
